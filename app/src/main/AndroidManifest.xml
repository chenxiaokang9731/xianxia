<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bright.readapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ui.application.App"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ui.activity.ZhihuWebActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ui.activity.PhotoActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ui.activity.GankDetailActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ui.activity.GankWebActivity"
            android:theme="@style/MainTheme" />
        <activity android:name=".ui.activity.DailyFeedActivity"
            android:theme="@style/MainTheme"
            />
    </application>

</manifest>